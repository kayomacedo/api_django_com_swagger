mkdir django_api_livros
cd django_api_livros

python -m venv venv
venv\Scripts\activate  # Windows
# ou: source venv/bin/activate (Linux/Mac)

📦 1. Instalar dependências

Ainda dentro do venv:

pip install django djangorestframework drf-yasg djangorestframework-simplejwt


Se quiser, já cria um requirements.txt:

pip freeze > requirements.txt



🏗️ 2. Criar o projeto core e o app livros

Dentro da pasta django_api_livros:

django-admin startproject core .
python manage.py startapp livros


Estrutura básica:

django_api_livros/
├── core/
│   ├── __init__.py
│   ├── asgi.py
│   ├── settings.py
│   ├── urls.py
│   └── wsgi.py
├── livros/
│   ├── __init__.py
│   ├── admin.py
│   ├── apps.py
│   ├── migrations/
│   ├── models.py
│   ├── tests.py
│   └── views.py
├── manage.py
└── venv/


⚙️ 3. Configurar core/settings.py

Abra core/settings.py e:

3.1. Adicione os apps

Em INSTALLED_APPS:

INSTALLED_APPS = [
    # padrão do Django
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',

    # terceiros
    'rest_framework',
    'drf_yasg',

    # apps do projeto
    'livros',
]   

3.2. Configurar DRF + JWT

Acrescente no final do arquivo:

from datetime import timedelta

REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': [
        'rest_framework_simplejwt.authentication.JWTAuthentication',
    ],
    'DEFAULT_PERMISSION_CLASSES': [
        'rest_framework.permissions.IsAuthenticated',  # tudo protegido por padrão
    ]
}

SIMPLE_JWT = {
    'ACCESS_TOKEN_LIFETIME': timedelta(minutes=60),
    'REFRESH_TOKEN_LIFETIME': timedelta(days=1),
}

3.3. Configuração do Swagger (drf-yasg)

Ainda no settings.py:

SWAGGER_SETTINGS = {
    'SECURITY_DEFINITIONS': {
        'Bearer': {
            'type': 'apiKey',
            'name': 'Authorization',
            'in': 'header',
            'description': 'Ex: Bearer <seu_token_jwt>'
        }
    }
}